call:
  core:
    mock-fs-enabled: true

spring:
  config:
    activate:
      on-profile: core
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/call_center?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 1!qazWSX
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: -1
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      validation-timeout: 5000
      connection-test-query: SELECT 1

freeswitch:
  enabled: true
  host: XXX
  port: 111
  password: XXXX
  timeout-seconds: 30
  heartbeat-timeout-seconds: 30
  max-retry-attempts: 3
  retry-interval-millis: 5000
  event-format: plain
  startup-events:
    - CHANNEL_CREATE
    - CHANNEL_HANGUP_COMPLETE
  originate-template: "user/%s"
